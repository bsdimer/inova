# Work log — 2026-09

## 2026-09-02 — Mobile surveys (Анкети) screen shell

**Changed:** Replaced the `ComingSoon` stub on `/surveys` with a glass UI matching the active/finished surveys mockup — glowing vote rings, status chips, compact finished rows. Still `MOCK` / `TODO(M7+)`.

**Verified:** `pnpm verify` green. Also marked an unmarked home payment `TODO(M4)` that was blocking `check:stubs`.

**Remains:** Survey detail / vote flow + real M7 API.

## 2026-09-06 — Voted-survey detail screen

**Changed:** New `/survey/[id]` screen (Анкета) for surveys the resident already voted on — glowing vote ring, "Защо е нужно?" card, options with radio + glowing result bars, "Промени гласа си" CTA, anonymity note. Extracted `VoteRing` into `src/components/VoteRing.tsx`; active survey cards now navigate to it. Mock data `TODO(M7+)`.

**Verified:** `pnpm verify` green; screen exercised in the iOS simulator via deep link.

**Remains:** Actual voting/re-voting flow, non-voted survey path, M7 API.

**Addendum:** Extracted the standard glowing linear bar into `src/components/GlowBar.tsx` (same SVG Gaussian bloom as `VoteRing`); survey option bars and the Каса deposit bar now use it. Verified via `pnpm verify` + simulator screenshot.

**Addendum 2:** Voting flow for non-voted surveys at `survey/[id]/vote` (results moved to `survey/[id]/index`) — paged 1–5 rating scales with glowing page segments, Назад/Продължи footer, mock submit → thank-you card → surveys. Results screen options are now tappable radios (local state); back buttons fall back when there is no navigation history. All `TODO(M7+)`.

## 2026-09-06 — Signals (Сигнали) tab screen

**Changed:** Replaced the `ComingSoon` stub in `(tabs)/issues.tsx` with the full Сигнали screen per mockup: brand header (no back arrow — it's a tab), "Сигнализирайте за нередност" form (category dropdown, multiline description, attach-photo row, black Изпрати CTA) and "История на сигналите" with working status filter chips and mock signal cards (badges, status dot, chevron). Form/data are `MOCK` / `TODO(M6)`.

**Verified:** `pnpm verify` green.

**Remains:** Submission + history from the M6 issues API, photo attach, signal detail screen.

**Addendum:** Photo marked optional ("Не е задължителна"); attach now uses `expo-image-picker` (gallery or camera, permission strings in `app.json`) with thumbnail + remove; Изпрати validates and prepends a local signal (status Докладван, today's date) and opens a frosted preview modal — history cards open the same preview. Still `TODO(M6)`. Verified via `pnpm verify` + simulator.
