name: inova

services:
  postgres:
    image: postgres:16-alpine
    container_name: inova-postgres
    environment:
      POSTGRES_USER: inova
      POSTGRES_PASSWORD: inova
      POSTGRES_DB: inova
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U inova']
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: inova-redis
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:latest
    container_name: inova-minio
    command: server /data --console-address ':9001'
    environment:
      MINIO_ROOT_USER: inova
      MINIO_ROOT_PASSWORD: inova-secret
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio-data:/data

  mailhog:
    image: mailhog/mailhog:latest
    container_name: inova-mailhog
    ports:
      - '1025:1025' # smtp
      - '8025:8025' # web ui

volumes:
  postgres-data:
  minio-data:
