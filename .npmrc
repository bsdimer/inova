# Public registry, explicitly: a developer whose user-level .npmrc points the
# default registry at another organisation's private registry must still be able
# to install this workspace.
registry=https://registry.npmjs.org/
